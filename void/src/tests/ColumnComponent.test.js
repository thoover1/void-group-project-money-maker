// const { mapReduxStateToProps } = require("../Components/Main/ColumnComponent");

// test("returning redux info", async () => {
//   // expect.assertions(1);
//   const data = await mapReduxStateToProps();
//   expect(data.user).toEqual(null);
// });
